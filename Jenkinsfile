pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'java -version'
                sh 'echo "pipeline test"'
		sh 'echo "Now with tag"'
            }
        }
    }
}
